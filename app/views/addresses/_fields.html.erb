<%= render 'shared/error_messages', object: f.object %>

<%= f.label "País: Venezuela" %><br/>
<%= f.hidden_field :location_type, :value => "Location" %>

<%= f.label "Estado" %><br/>
<%= collection_select(:address, :id, Address.find(:all), :id, :address_name, {:prompt => "Select a State"}, {:id => 'states_select'}) %>
<%= collection_select(:address, :belong_address, @cities, :id, :address_name, {:prompt => "Select an City"}, {:id => 'city_select'})%>

<%= f.label :address_name%>
<%= f.text_area :address_name %>