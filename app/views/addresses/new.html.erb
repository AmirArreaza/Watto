<h1>Direccion</h1>

<div class="row">
	<div class="span4 offset4">
    	<%= form_for(@address) do |f| %>

    		<%= render 'fields', f:f %>

      		<%= f.submit "Agregar Direccion", class: "btn btn-success" %>

    	<% end %>
  	</div>
</div>