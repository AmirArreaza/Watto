<h1>Editando: <%= @company.commercial_name %></h1>

<div class="row">
	<div class="span4 offset4">
    <%= form_for(@company) do |f| %>
      <%= render 'fields', f:f %>
      <%= f.submit "Guardar cambios", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
