<h1>Agrega una compañía</h1>

<div class="row">
	<div class="span4 offset4">
    <%= form_for(@company) do |f| %>
      <%= render 'fields', f:f %>
      <%= f.submit "Crear compañía", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
