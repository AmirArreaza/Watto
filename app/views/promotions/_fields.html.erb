<%= render 'shared/error_messages', object: f.object %>

<%= f.collection_select :promotion_type_id, PromotionType.all, :id, :name, :prompt => "- Tipo de Promocion -" %>

<%= f.datepicker(:begin_date, :readonly=>'true', :dateFormat => 'dd-mm-yy')  %>

<%= hidden_field_tag "company", @company.id %>