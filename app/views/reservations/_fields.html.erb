<%= render 'shared/error_messages', object: f.object %>

<%= f.label :quantity%>
<%= f.text_field :quantity %>

<%= f.label :product_id%>
<%= f.collection_select :product_id, Product.all, :id, :name, :prompt => "Seleccione un producto" %>