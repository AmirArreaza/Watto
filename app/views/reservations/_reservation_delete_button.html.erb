<% if current_user?(item.user) %>
	<%= button_to "Eliminar Reservación", item, class: "btn btn-danger", method: :delete,
    	data: { confirm: "You sure?" },
    	title: item.product.name %>
<% end %>