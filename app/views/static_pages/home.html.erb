<h1>Home Page</h1>

<h2>Ingresa</h2>

<% if signed_in? %>
	<h1>Bienvenido a TITULO</h1>
	<h2><%= link_to "Sign out", signout_path, method: "delete" %></h2>
<% else %>
	<div class="center hero-unit">
		<h1>Bienvenido a TITULO</h1>
  		<aside>
  			<section>
  				<%= render 'shared/sessions_new' %>
  			</section>
  		</aside>
	</div>
<% end %> 
