<%= render 'shared/error_messages', object: f.object %>

<%= f.label :department_id %>
<%= f.collection_select :department_id, Department.all, :id, :name, :prompt => "- Seleccione Departamento -" %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_area :description %>