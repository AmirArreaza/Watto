<h1>Regístrate</h1>

<div class="row">
	<div class="span4 offset4">
    	<%= form_for(@user) do |f| %>
            <%= render 'fields', f:f %>
      		<%= f.submit "Crear mi cuenta", class: "btn btn-success" %>
    	<% end %>
  	</div>
</div>